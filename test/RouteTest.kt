import Locations.Factory
import Locations.WarehouseB
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class RouteTest {
    @Test
    fun `route from factory to B spend 5 hours to travel`() {
//        assertThat(Route(Place(), WarehouseB, Time.of(5)).timeToTravel(Factory, WarehouseB)).isEqualTo(Time.of(5))
    }
}
